
admin:#############################################

# htpasswd -c .htpasswd <username>
# kubectl create secret generic loki-basic-auth --from-file=.htpasswd -n prod-monitoring
# kubectl create secret generic canary-basic-auth --from-literal=username=admin --from-literal=password=admin -n prod-monitoring
# Ensure Loki uses this secret in the Helm values - Modify your values.yaml (if using Helm) to mount the secret into Loki

# for better understanding Following this document: https://grafana.com/docs/loki/latest/setup/install/helm/deployment-guides/azure/
